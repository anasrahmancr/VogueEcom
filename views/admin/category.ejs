<!-- <!DOCTYPE html>
<html>-->
  <head>
    <title>Category Management</title> 
    <style>

th {
  background-color: #500089;
  color: white;
  font-size: 16px;
  padding-left: 10px;
  border-bottom: 2px solid white; 
      }


table {
  border-collapse: collapse;
  width: 100%;
  border-radius: 1em;
  overflow: hidden;
}

 td {
  padding: 1em;
  /* background: #ddd; */
  
}

body {
  margin: 1.5em;
}

      label {
        margin-top: 10px;
        font-weight: bold;
      }
      input[type="text"],
      select {
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 5px;
        margin-top: 5px;
        margin-bottom: 10px;
        font-size: 16px;
        background-color: rgb(211, 211, 211);
      }
      button[type="submit"] {
        background-color: #565656;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
        margin-top: 10px;
        margin-bottom: 10px;
      }
      button[type="submit"]:hover {
        background-color: #2f2e2e;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        background-color: #ffff;
      }
      
tr:nth-child(odd) {
  background-color: #f2f2f2;
}

tr:nth-child(even) {
  background-color: #ffffff;
}
      /* * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: sans-serif;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      h1 {
        text-align: center;
        margin-bottom: 20px;
      }
      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        background-color: #f2f2f2;
        padding: 20px;
        border-radius: 10px;
      }
      label {
        margin-top: 10px;
        font-weight: bold;
      }
      input[type="text"],
      select {
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 5px;
        margin-top: 5px;
        margin-bottom: 10px;
        font-size: 16px;
      }
      button[type="submit"] {
        background-color: #4caf50;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
        margin-top: 10px;
      }
      button[type="submit"]:hover {
        background-color: #3e8e41;
      }
      table {
        margin-top: 20px;
        width: 100%;
        border-collapse: collapse;
      }
      th,
      td {
        border: 1px solid black;
        padding: 10px;
        text-align: center;
      }
      th {
        background-color: #4caf50;
        color: white;
        font-size: 16px;
      }
      td a {
        color: blue;
        text-decoration: underline;
      } */
    </style>
  </head>
  <body>
    <div class="container">
      <h1 style="color: #500089; opacity: 0.7;">Category Management</h1>
      <form method="post" action="/admin/addCategory">
        <label for="category-name">Category Name:</label>
        <input type="text" id="category-name" name="categoryname" required />

        <button type="submit">Add Category</button>
      </form>

      <table>
        <thead>
          <tr>
            <th style=" padding-left: 10px;">Category Name</th>
            <th style=" padding-left: 10px;">Action</th>
          </tr>
        </thead>
        <tbody>
          
          <% (categories || []).forEach(category => { %>
          <tr>
            <td><%= category.categoryName %></td>
            <td><a href="/deleteCategory/<%= category._id %>">Delete</a></td>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  </body>
</html>
